  <div class="row">
    <div class="col-md-12">
      <ul class="list-group">
        <% @categories.each do |category| %>
          <%= link_to category.name, {id: category.id, action: 'show'}, {class: 'list-group-item'} %>
        <% end %>
      </ul>
    </div>
  </div>